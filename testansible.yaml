---
  - hosts : cp-platformtst6
    become: true
    tasks :
    - name : install nginx
      package : pkg=nginx state=installed
     
      notify:
      - start nginx
    - name : message 
      command : echo "installed nginx!"
    handlers :
    - name : start nginx
      service : name=nginx state=started


